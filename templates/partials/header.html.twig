<header class="headerSite">
    <div class="wrapper">
        <div class="logo">
            <a href="/"><img src="/logo/Union.png" alt="Logo coup de patte"></a>
        </div>
        <nav>
            <ul>
                <li>
                    <a href="{{path("home")}}" class="menuUpperHeader">Menu</a>
                    {# <ul>
                        <li><a href="{{path("home")}}">Accueil</a></li>
                        <li><a href="#pres">Présentation</a></li>
                        <li><a href="#pensionnaires">Animaux</a></li>
                        <li><a href="#contact">Contact</a></li>
                        <li><a href="{{path('donation_one')}}">Faire un don</a></li>
                        {% if app.user %}
                            <li><a href="{{path('account_logout')}}">Déconnexion</a></li>
                        {% else %}
                            <li><a href="{{path('account_register')}}">Inscription</a></li>
                            <li><a href="{{path('account_login')}}">Connexion</a></li>
                        {% endif %}
                        
                    </ul> #}
                </li>
                <li>
                    <h6>Animaux</h6>
                    <ul>
                        <li><a href="{{path('animal_index',{'type':'chien'})}}">Chien</a></li>
                        <li><a href="{{path('animal_index',{'type':'chat'})}}">Chat</a></li>
                        <li><a href="{{path('animal_index',{'type':'lapin'})}}">Lapin</a></li>
                    </ul>
                </li>
                <li><a href="{{path('blog')}}">Blog</a></li>
                <li><a href="{{path('adoption_one')}}">Faire adopter</a></li>
                <li><a href="{{path('cart')}}"><i class="fa-solid fa-bag-shopping"></i><span class="numberAchatHeader ms-2">0</span></a></li>
                <li>
                    <i class="fa-solid fa-user"></i>
                    <ul>
                        {% if app.user %}
                            <li><a href="{{path('account_index')}}">Profil</a></li>
                            <li><a href="{{path('parrainage_index')}}">Parrainage</a></li>
                            <li><a href="{{path('donation_one')}}">Faire un don</a></li>
                            <li><a href="{{path('account_logout')}}">Déconnexion</a></li>
                        {% else %}
                            <li><a href="{{path('account_register')}}">Inscription</a></li>
                            <li><a href="{{path('account_login')}}">Connexion</a></li>
                            <li><a href="{{path('donation_one')}}">Faire un don</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
            <div id="burger">
                <div class="bar"></div>
                <div class="bar"></div>
                <div class="bar"></div>
            </div>
        </nav>
    </div>
</header>
<div class="underHeader">
    <div class="wrapper">
        <div class="linkUnderHeader">
            <a href="{{path("home")}}">Accueil</a>
            <a href="{{path("home")}}#pres">Présentation</a>
            <a href="{{path("home")}}#pensionnaires">Pensionnaires</a>
            <a href="{{path("home")}}#contact">Contact</a>
        </div>
        <div class="linkUnderHeader">
            <a href="{{path('animal_index',{'type':'chien'})}}">Chien</a>
            <a href="{{path('animal_index',{'type':'chat'})}}">Chat</a>
            <a href="{{path('animal_index',{'type':'lapin'})}}">Lapin</a>
        </div>
        <div class="linkUnderHeader">
            {% if app.user %}
                <a href="{{path('account_index')}}">Profil</a></li>
                <a href="{{path('parrainage_index')}}">Parrainage</a>
                <a href="{{path('donation_one')}}">Faire un don</a>
                <a href="{{path('account_logout')}}">Déconnexion</a>
            {% else %}
                <a href="{{path('account_register')}}">Inscription</a>
                <a href="{{path('account_login')}}">Connexion</a>
                <a href="{{path('donation_one')}}">Faire un don</a>
            {% endif %}
        </div>
    </div>
</div>
<div class="slide" id="menu">
    <div class="left"></div>
    <div class="right">
        <div class="menuLink">
            <div class="menuContainer">
                <a href="{{path("home")}}">Menu</a>
                <i class="fa-solid fa-chevron-right chevronMenu"></i>
            </div>
            <div class="link">
                <a href="{{path("home")}}">Accueil</a>
                <a href="#pres">Présentation</a>
                <a href="#pensionnaires">Animaux</a>
                <a href="#contact">Contact</a>
                <a href="{{path('donation_one')}}">Faire un don</a>
                {% if app.user %}
                    <a href="{{path('account_logout')}}">Déconnexion</a>
                {% else %}
                    <a href="{{path('account_register')}}">Inscription</a>
                    <a href="{{path('account_login')}}">Connexion</a>
                {% endif %}
            </div>
        </div>
        <div class="menuLink">
            <div class="menuContainer">
                <h6>Animaux</h6>
                <i class="fa-solid fa-chevron-right chevronMenu"></i>
            </div>
            <div class="link">
                <a href="{{path('animal_index',{'type':'chien'})}}">Chien</a>
                <a href="{{path('animal_index',{'type':'chat'})}}">Chat</a>
                <a href="{{path('animal_index',{'type':'lapin'})}}">Lapin</a>
            </div>
        </div>
        <div class="menuLink">
            <a href="{{path('blog')}}">Blog</a>
        </div>
        <div class="menuLink">
            <a href="{{path('adoption_one')}}">Faire adopter</a>
        </div>
        <div class="menuLink">
            <a href="{{path('cart')}}"><i class="fa-solid fa-bag-shopping"></i></a>
            <span class="numberAchatHeader ms-2">0</span>
        </div>
        <div class="menuLink">
            <div class="menuContainer">
                <i class="fa-solid fa-user"></i>
                <i class="fa-solid fa-chevron-right chevronMenu"></i>
            </div>
            <div class="link">
                {% if app.user %}
                    <a href="{{path('donation_one')}}">Faire un don</a>
                    <a href="{{path('account_index')}}">Profil</a>
                    <a href="{{path('parrainage_index')}}">Parrainage</a>
                    <a href="{{path('account_logout')}}">Déconnexion</a>
                {% else %}
                    <a href="{{path('account_register')}}">Inscription</a>
                    <a href="{{path('account_login')}}">Connexion</a>
                    <a href="{{path('donation_one')}}">Faire un don</a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
